import React from 'react';
import './User.css';

export default class User extends React.Component {
    render() {
        return (
            <div>
                <p> This is the user systme page. which is the user interface, the client side data profile.  </p>
                <h1 className="hello">Hello User system.</h1>
            </div>
        )
    }
}